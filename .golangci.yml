version: "2"

linters:
  default: none
  enable:
    - govet
    - misspell
  settings:
    misspell:
      locale: UK
      ignore-rules:
        - color
        - organization

formatters:
  enable:
    - gofmt
